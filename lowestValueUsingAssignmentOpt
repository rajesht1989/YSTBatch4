// write a program to get no from user and find lowest value using assignment opertor print it on console
#include <stdio.h>
int getNumberFormUser(){
    int i;
    scanf("%d",&i);
    return i;
}
int findLowestValue(int a,int b){
    return a>b;
}

int main() {
      printf("Enter a first value ");
      int number1 = getNumberFormUser();
    printf("Enter A Second value ");
       int number2 = getNumberFormUser();
int lowest = findLowestValue(number1,number2);
  printf("value is %d",lowest);
    return 0;
}
